import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const OperatorHome = () => {
  return (
    <SafeAreaView className="flex-1 items-center bg-background"></SafeAreaView>
  );
};

export default OperatorHome;
